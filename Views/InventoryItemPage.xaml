<Page x:Class="AdminUP.Views.InventoryItemPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Loaded="Page_Loaded">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Добавить" Width="110" Margin="0,0,8,0" Click="AddButton_Click"/>
            <Button Content="Редактировать" Width="130" Margin="0,0,8,0" Click="EditButton_Click"/>
            <Button Content="Удалить" Width="110" Margin="0,0,16,0" Click="DeleteButton_Click"/>

            <TextBlock Text="Инвентаризация:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="InventoryComboBox"
                      Width="240"
                      DisplayMemberPath="name"
                      SelectedValuePath="id"
                      Margin="0,0,16,0"
                      SelectionChanged="InventoryComboBox_SelectionChanged"/>

            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="SearchTextBox"
                     Width="220"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,8,0"/>

            <Button Content="Найти" Width="90" Margin="0,0,8,0" Click="SearchButton_Click"/>
            <Button Content="Очистить" Width="100" Click="ClearButton_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding FilteredInventoryItemList}"
                  SelectedItem="{Binding SelectedInventoryItem}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="70"/>
                <DataGridTextColumn Header="Инвентаризация ID" Binding="{Binding inventory_id}" Width="160"/>
                <DataGridTextColumn Header="Оборудование ID" Binding="{Binding equipment_id}" Width="150"/>
                <DataGridTextColumn Header="Проверил (user_id)" Binding="{Binding checked_by_user_id}" Width="160"/>
                <DataGridTextColumn Header="Комментарий" Binding="{Binding comment}" Width="*"/>
                <DataGridTextColumn Header="Дата проверки" Binding="{Binding checked_at, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="170"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>