<Window x:Class="AdminUP.Views.EditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AdminUP.Views"
         mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Контент редактирования -->
        <ContentControl x:Name="EditContentControl" Grid.Row="0" Margin="10"/>

        <!-- Панель статуса -->
        <Border Grid.Row="1" Background="#F5F5F5" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                <TextBlock x:Name="StatusText" Foreground="Gray" Margin="0,0,10,0"/>
                <Button x:Name="SaveButton" Content="Сохранить" Width="100" 
                        Click="SaveButton_Click" Margin="0,0,5,0"/>
                <Button x:Name="CancelButton" Content="Отмена" Width="100" 
                        Click="CancelButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
