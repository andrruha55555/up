<Page x:Class="AdminUP.Views.EquipmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:converters="clr-namespace:AdminUP.Converters"
      mc:Ignorable="d" 
      Title="Оборудование"
      Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,8">
            <Button Content="Добавить" Width="100" Click="AddButton_Click"/>
            <Button Content="Редактировать" Width="120" Click="EditButton_Click"/>
            <Button Content="Удалить" Width="100" Click="DeleteButton_Click"/>
            <Separator Width="1" Margin="10,0" Background="#DDDDDD"/>
            <Button Content="📊 Экспорт в Excel" Width="140" Click="ExportButton_Click"
                    Background="#1D6F42" Foreground="White" FontWeight="SemiBold"/>
            <Separator Width="1" Margin="10,0" Background="#DDDDDD"/>
            <TextBox x:Name="SearchTextBox" Width="220" Margin="0,0,5,0" 
                     VerticalContentAlignment="Center"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Найти" Width="80" Click="SearchButton_Click"/>
            <Button Content="Очистить" Width="80" Margin="5,0,0,0" Click="ClearButton_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="1" Margin="10"
                  ItemsSource="{Binding FilteredEquipmentList}"
                  SelectedItem="{Binding SelectedEquipment}"
                  AutoGenerateColumns="False"
                  CanUserSortColumns="True"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" SortMemberPath="Id" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" SortMemberPath="Name" Width="200"/>
                <DataGridTextColumn Header="Инв.номер" Binding="{Binding InventoryNumber}" SortMemberPath="InventoryNumber" Width="120"/>
                <DataGridTextColumn Header="Аудитория" Binding="{Binding ClassroomName}" SortMemberPath="ClassroomName" Width="110"/>
                <DataGridTextColumn Header="Ответственный" Binding="{Binding ResponsibleUser}" SortMemberPath="ResponsibleUser" Width="160"/>
                <DataGridTextColumn Header="Стоимость" Binding="{Binding Cost}" SortMemberPath="Cost" Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding StatusName}" SortMemberPath="StatusName" Width="100"/>
                <DataGridTextColumn Header="Модель" Binding="{Binding ModelName}" SortMemberPath="ModelName" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>