<Page x:Class="AdminUP.Views.EquipmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:converters="clr-namespace:AdminUP.Converters"
      mc:Ignorable="d" 
      Title="Оборудование"
      Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Добавить" Width="100" Click="AddButton_Click"/>
            <Button Content="Редактировать" Width="120" Click="EditButton_Click"/>
            <Button Content="Удалить" Width="100" Click="DeleteButton_Click"/>
            <TextBox x:Name="SearchTextBox" Width="200" Margin="10 0" 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Найти" Width="80" Click="SearchButton_Click"/>
            <Button Content="Очистить" Width="80" Margin="5,0,0,0" Click="ClearButton_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="1" Margin="10"
                  ItemsSource="{Binding FilteredEquipmentList}"
                  SelectedItem="{Binding SelectedEquipment}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding name}" Width="200"/>
                <DataGridTextColumn Header="Инв.номер" Binding="{Binding inventory_number}" Width="120"/>
                <DataGridTextColumn Header="Аудитория" Binding="{Binding classroom_id}" Width="90"/>
                <DataGridTextColumn Header="Ответственный" Binding="{Binding responsible_user_id}" Width="120"/>
                <DataGridTextColumn Header="Стоимость" Binding="{Binding cost, StringFormat={}{0:N2}}" Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding status_id}" Width="90"/>
                <DataGridTextColumn Header="Модель" Binding="{Binding model_id}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>