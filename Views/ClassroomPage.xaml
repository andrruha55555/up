<Page x:Class="AdminUP.Views.ClassroomPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="Аудитории"
      Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Добавить" Width="100" Click="AddButton_Click"/>
            <Button Content="Редактировать" Width="120" Click="EditButton_Click" Margin="5,0,0,0"/>
            <Button Content="Удалить" Width="100" Click="DeleteButton_Click" Margin="5,0,0,0"/>

            <TextBox x:Name="SearchTextBox" Width="220" Margin="15,0,0,0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Content="Найти" Width="80" Margin="5,0,0,0" Click="SearchButton_Click"/>
            <Button Content="Очистить" Width="90" Margin="5,0,0,0" Click="ClearButton_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="1" Margin="10"
                  ItemsSource="{Binding FilteredClassroomList}"
                  SelectedItem="{Binding SelectedClassroom}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="60"/>
                <DataGridTextColumn Header="Название" Binding="{Binding name}" Width="*"/>
                <DataGridTextColumn Header="Сокращение" Binding="{Binding short_name}" Width="120"/>
                <DataGridTextColumn Header="Ответственный" Binding="{Binding responsible_user_id}" Width="140"/>
                <DataGridTextColumn Header="Врем. ответственный" Binding="{Binding temp_responsible_user_id}" Width="160"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>