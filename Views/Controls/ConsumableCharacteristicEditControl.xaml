<UserControl x:Class="AdminUP.Views.Controls.ConsumableCharacteristicEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdminUP.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="500">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Редактирование характеристики" Style="{StaticResource HeaderText}"/>

        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Расходник:*" Style="{StaticResource LabelText}"/>
            <ComboBox Grid.Row="0" Grid.Column="1" 
                      ItemsSource="{Binding AvailableConsumables}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding ConsumableId}"
                      Margin="5"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Название характеристики:*" Style="{StaticResource LabelText}"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding CharacteristicName, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="5" MaxLength="100" Tag="Например: Цвет, Ресурс, Совместимость"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Значение:*" Style="{StaticResource LabelText}"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CharacteristicValue, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="5" Height="60" TextWrapping="Wrap" AcceptsReturn="True"
                     Tag="Введите значение характеристики"/>
        </Grid>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding ValidationErrors}" 
                      Margin="5" Visibility="{Binding HasErrors, Converter={StaticResource BoolToVis}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Style="{StaticResource ErrorText}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
