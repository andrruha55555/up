<UserControl x:Class="AdminUP.Views.Controls.InventoryEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="560" d:DesignWidth="620">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Поля инвентаризации -->
        <Border Grid.Row="0" Background="White" Padding="16,14"
                BorderBrush="#E0E4EA" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Наименование *"
                           FontSize="12" FontWeight="SemiBold" Foreground="#546E7A"
                           VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="4"
                         Height="34" FontSize="13" Padding="8,0"
                         VerticalContentAlignment="Center"
                         BorderBrush="#CFD4DC" BorderThickness="1"
                         Text="{Binding InventoryName, UpdateSourceTrigger=PropertyChanged}"
                         MaxLength="100"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Дата начала *"
                           FontSize="12" FontWeight="SemiBold" Foreground="#546E7A"
                           VerticalAlignment="Center"/>
                <DatePicker Grid.Row="2" Grid.Column="1" Height="34"
                            SelectedDate="{Binding StartDate}"
                            FirstDayOfWeek="Monday"/>

                <TextBlock Grid.Row="2" Grid.Column="3" Text="Дата окончания *"
                           FontSize="12" FontWeight="SemiBold" Foreground="#546E7A"
                           VerticalAlignment="Center"/>
                <DatePicker Grid.Row="2" Grid.Column="4" Height="34"
                            SelectedDate="{Binding EndDate}"
                            FirstDayOfWeek="Monday"/>
            </Grid>
        </Border>

        <!-- Тулбар выбора оборудования -->
        <Border Grid.Row="1" Background="#F8F9FB" Padding="12,8"
                BorderBrush="#E0E4EA" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Выберите оборудование для инвентаризации:"
                               FontSize="12" FontWeight="SemiBold" Foreground="#546E7A"
                               VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <Border Background="#EEF4FF" CornerRadius="10" Padding="8,3">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Выбрано: " FontSize="12" Foreground="#546E7A"/>
                            <TextBlock Text="{Binding SelectedEquipmentCount}"
                                       FontSize="12" FontWeight="Bold" Foreground="#1565C0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Все" Width="50" Height="28" FontSize="12"
                            Margin="0,0,4,0" Cursor="Hand"
                            Background="#1565C0" Foreground="White" BorderThickness="0"
                            Click="SelectAll_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="6,0">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#0D47A1"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button Content="Снять" Width="54" Height="28" FontSize="12"
                            Cursor="Hand"
                            Background="#546E7A" Foreground="White" BorderThickness="0"
                            Click="DeselectAll_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="6,0">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#37474F"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Список оборудования -->
        <Border Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- Заголовок -->
                <Grid Grid.Row="0" Background="#F0F2F5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="36"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="1" Text="Оборудование" FontSize="11"
                               FontWeight="SemiBold" Foreground="#546E7A" Margin="4,6"/>
                    <TextBlock Grid.Column="2" Text="Инв. номер" FontSize="11"
                               FontWeight="SemiBold" Foreground="#546E7A" Margin="4,6"/>
                    <TextBlock Grid.Column="3" Text="Кабинет" FontSize="11"
                               FontWeight="SemiBold" Foreground="#546E7A" Margin="4,6"/>
                </Grid>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding EquipmentItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="4,4" BorderBrush="#F0F2F5" BorderThickness="0,0,0,1">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="White"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                    <Setter Property="Background" Value="#F0FFF4"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="36"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="100"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox Grid.Column="0"
                                                  IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding EquipmentName}"
                                                   FontSize="13" VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis" Margin="4,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#546E7A"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                            <Setter Property="Foreground" Value="#1B5E20"/>
                                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding InventoryNumber}"
                                                   FontSize="12" FontFamily="Consolas"
                                                   Foreground="#9E9E9E"
                                                   VerticalAlignment="Center" Margin="4,0"/>
                                        <TextBlock Grid.Column="3"
                                                   Text="{Binding ClassroomName}"
                                                   FontSize="12" Foreground="#546E7A"
                                                   VerticalAlignment="Center" Margin="4,0"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Ошибки валидации -->
        <ItemsControl Grid.Row="3" ItemsSource="{Binding ValidationErrors}" Margin="12,4">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Foreground="#C62828" FontSize="12"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
