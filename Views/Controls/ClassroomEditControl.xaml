<UserControl x:Class="AdminUP.Views.Controls.ClassroomEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="500">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Название:*" />
        <TextBox Grid.Row="0" Grid.Column="1"
                 Margin="5"
                 Text="{Binding ClassroomName, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Сокращение:*" />
        <TextBox Grid.Row="1" Grid.Column="1"
                 Margin="5"
                 MaxLength="20"
                 Text="{Binding ClassroomShortName, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Ответственный:" />
        <ComboBox Grid.Row="2" Grid.Column="1"
                  Margin="5"
                  ItemsSource="{Binding AvailableUsers}"
                  SelectedValuePath="id"
                  SelectedValue="{Binding ResponsibleUserId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <Run Text="{Binding last_name}"/>
                        <Run Text=" "/>
                        <Run Text="{Binding first_name}"/>
                        <Run Text=" "/>
                        <Run Text="{Binding middle_name}"/>
                    </TextBlock>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Временный ответственный:" />
        <ComboBox Grid.Row="3" Grid.Column="1"
                  Margin="5"
                  ItemsSource="{Binding AvailableUsers}"
                  SelectedValuePath="id"
                  SelectedValue="{Binding TempResponsibleUserId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <Run Text="{Binding last_name}"/>
                        <Run Text=" "/>
                        <Run Text="{Binding first_name}"/>
                        <Run Text=" "/>
                        <Run Text="{Binding middle_name}"/>
                    </TextBlock>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                   Margin="0,10,0,0"
                   Foreground="Gray"
                   FontStyle="Italic"
                   Text="* - обязательные поля" />

        <ItemsControl Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                      Margin="0,10,0,0"
                      ItemsSource="{Binding ValidationErrors}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Foreground="Red"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>