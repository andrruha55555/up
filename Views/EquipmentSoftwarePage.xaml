<Page x:Class="AdminUP.Views.EquipmentSoftwarePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:converters="clr-namespace:AdminUP.Converters"
      mc:Ignorable="d" 
      Title="Оборудование-ПО"
      Loaded="Page_Loaded">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Добавить" Width="100" Click="AddButton_Click"/>
            <Button Content="Редактировать" Width="120" Click="EditButton_Click"/>
            <Button Content="Удалить" Width="100" Click="DeleteButton_Click"/>
            <ComboBox x:Name="EquipmentComboBox" Width="150" Margin="10 0" 
                      SelectionChanged="EquipmentComboBox_SelectionChanged"
                      DisplayMemberPath="Name" SelectedValuePath="Id"/>
            <ComboBox x:Name="SoftwareComboBox" Width="150" 
                      SelectionChanged="SoftwareComboBox_SelectionChanged"
                      DisplayMemberPath="Name" SelectedValuePath="Id"/>
            <TextBox x:Name="SearchTextBox" Width="200" 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Найти" Width="80" Click="SearchButton_Click"/>
            <Button Content="Очистить" Width="80" Margin="5,0,0,0" Click="ClearButton_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="1" Margin="10"
                 ItemsSource="{Binding FilteredEquipmentSoftwareList}"
                 SelectedItem="{Binding SelectedEquipmentSoftware}"
                 AutoGenerateColumns="False"
                 IsReadOnly="True"
                 SelectionMode="Single"
                 SelectionUnit="FullRow"
                 MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Оборудование ID" Binding="{Binding EquipmentId}" Width="100"/>
                <DataGridTextColumn Header="ПО ID" Binding="{Binding SoftwareId}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Индикатор загрузки -->
        <Border Grid.Row="0" Grid.RowSpan="2" 
                Background="#88000000" 
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20"/>
                <TextBlock Text="Загрузка данных..." Foreground="White" Margin="0 10 0 0"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>