<Page x:Class="AdminUP.Views.EquipmentHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:converters="clr-namespace:AdminUP.Converters"
      mc:Ignorable="d" 
      Title="История оборудования"
      Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Добавить" Width="100" Click="AddButton_Click"/>
            <Button Content="Редактировать" Width="120" Click="EditButton_Click"/>
            <Button Content="Удалить" Width="100" Click="DeleteButton_Click"/>

            <ComboBox x:Name="EquipmentComboBox" Width="200" Margin="10 0"
                      SelectionChanged="EquipmentComboBox_SelectionChanged"
                      DisplayMemberPath="name"
                      SelectedValuePath="id"/>

            <TextBox x:Name="SearchTextBox" Width="200"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Найти" Width="80" Click="SearchButton_Click"/>
            <Button Content="Очистить" Width="80" Margin="5,0,0,0" Click="ClearButton_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="1" Margin="10"
                  ItemsSource="{Binding FilteredHistoryList}"
                  SelectedItem="{Binding SelectedHistory}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="50"/>
                <DataGridTextColumn Header="Оборудование ID" Binding="{Binding equipment_id}" Width="120"/>
                <DataGridTextColumn Header="Аудитория" Binding="{Binding classroom_id}" Width="90"/>
                <DataGridTextColumn Header="Ответственный" Binding="{Binding responsible_user_id}" Width="120"/>
                <DataGridTextColumn Header="Комментарий" Binding="{Binding comment}" Width="250"/>
                <DataGridTextColumn Header="Дата изменения" Binding="{Binding changed_at, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="160"/>
                <DataGridTextColumn Header="Изменил" Binding="{Binding changed_by_user_id}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>